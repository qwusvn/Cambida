Parent cambida-modular-camera-20260924: BLOCKED AT INTEGRATION; partial source produced, NOT COMPLETED. Baseline main HEAD 7466c56 initially clean. No tests/build/device trials/release/restart authorized or performed.
AGY MCP task IDs: agy_muezj3oo_5f24d1, agy_muezjddm_4e97f1, agy_muezju51_3ae56b, agy_muezk1g2_b3d707. MCP workers returned status without verified file output. AGY CLI independent scoped runs wrote untracked core/config/__init__/hikvision/rtsp/onvif modules; one CLI attempt returned 503 eligibility, others returned print timeout while remote turns continued. YATO executor.start AGY fails because runner passes unsupported --workspace flag. No module is wired into running Cambida; no admin UI, privacy switch, channel discovery/bulk-add, table limit or license upgrade delivered yet.
Do not mark agents or parent COMPLETED until results reviewed, missing policy/discovery modules finished, architecture reconciled, backend/front-end integrated, Git scoped review completed. On recovery first inspect active AGY CLI sessions and current file mtimes to prevent duplicate edits; preserve existing config/media/QR/release and live service. Check the source before any future task continuation.

## Checkpoint 2026-09-24 — partial verified integration (NOT RELEASED)
- AGY CLI home task e6555e7f-cd1b-4087-8873-410233827ea4 wrote `home.html` toggle/edit UI. AGY backend task 40900bbb-728b-48df-a8b2-130e0ca9fbbb and admin-camera task 76185d76-b99b-4958-9c0f-8fcfe5e91f4d terminated after print timeout WITHOUT writing their allocated files. Do not infer task completion from exit_code=0.
- YATO implemented modular `camera_modules/table_access.py`, mounted in `1.py`, providing authenticated table GET/toggle/reorder, admin session JSON login, server-side guest denies for per-camera live/replay/list/snapshot/cut/download/NVR token and aggregate feeds when any table is disabled. Stable ID (`ban-01` etc.) mapped from the existing table configuration into home cards; red = enabled and grey = disabled. Direct legacy cut filenames with unknown owner are conservatively denied to guests while any table is disabled. Existing recording workers are untouched.
- Added verified Telegram-hardware `/N` cap check to both ordinary config PUT and config restore, only on table count growth; preserving old IDs and QR. Actual live Telegram grant and physical device tests have NOT been run, and all new routes have NOT been exercised via a restarted live service.
- YATO tests: `python -m pytest -q tests` => 128 passed, 1 requests dependency warning, 3 subtests passed; JS syntax and Python compile pass. Old test_v2 file-list check had a hardcoded recording date outside the actual endpoint default last-24-hour window; explicitly supplied its historical date in the test. Port 8000 of Hikvision 192.168.1.199 TCP reachable. Hikvision HCNetSDK.dll unavailable (`hcnetsdk_available() = False`), so SDK channel enumeration and true live recording trial are BLOCKED.
- Additional YATO backend `POST /api/admin/camera/probe` now dispatches to available Hikvision, Dahua/Imou and ONVIF adapter probes and reports missing SDK/capability explicitly, and authenticated `GET /api/admin/camera/discover` calls existing LAN discovery without inventing channels. `rtsp`/`kbvision` probe currently reports 501 (unverified). Both APIs are implemented in `camera_modules/table_access.py`, have isolated mocked regression tests, and have not been device-qualified.
- Remaining: admin camera-add UI `admin.html` is unchanged; device-channel bulk-add API and end-to-end vendor SDK integration remain unimplemented. Camera auto selection/browser H.265 Safari adaptive playback not addressed. No release, server restart, configuration/media/QR rewrite, or git commit. Parent remains PARTIAL / BLOCKED, NOT COMPLETED. Do not start duplicate AGY on `1.py` without checking current modified source.

## Checkpoint 2026-09-24 13:50 Asia/Bangkok — YATO integration and tests (PARTIAL; NOT RELEASED)
- AGY CLI finished editing admin.html. The new add-device form supports vendor/host/port/credentials, authenticated LAN discovery, real SDK channel probe, multi-channel selection and staged Dahua/Imou NetSDK cameraDrafts. The form reports unsupported/missing SDK or unavailable stream mapping and does not invent a channel. Old existing camera editing remains intact. Adding a device does not write config until an explicit admin save. YATO tightened channel ID validation and duplicate guard.
- YATO removed the four competing legacy Flask route registrations in 1.py so camera_modules/table_access.py owns authenticated /api/admin/camera/probe, GET /api/admin/camera/discover, GET /api/admin/tables and POST /api/admin/tables/reorder. Legacy unverified /api/admin/camera/bulk-add returns HTTP 501 rather than writing fake camera configuration. No runtime config, recording files, QR data, release binaries or deployment were modified by these implementation steps.
- New regression tests/test_camera_route_integration.py asserts canonical routing, admin/cross-origin protection and blocked bulk-add. Expanded tests/test_table_access.py for first-install legacy cameras with missing tables: admin can switch a table off; red means on, grey means off; guest camera route is denied; reorder preserves camera identity. Latest full YATO test run: 132 passed, 3 subtests passed, 1 requests dependency warning. Inline admin JavaScript syntax passed Node check during integration; recheck after final edits.
- Hikvision network device 192.168.1.199 TCP port 8000 reachable, but HCNetSDK.dll absent from project: cannot verify real SDK channel enumeration or end-to-end recording. Dahua/Imou SDK add-flow has only mocked/structural tests, no verified physical Dahua recorder in this environment. Safari H.265 adaptive live playback, device-profile learning and iPhone-specific playback/download still NOT implemented or qualified. iPhone hardware was not tested per user's instruction. No service restart, live config switch, release package or git commit; parent remains PARTIAL / BLOCKED pending actual vendor SDK library and device qualification and Safari playback engineering.
