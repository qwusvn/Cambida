TASK ID: cambida-camera-transport-clean-20260919. User authorized implementation AND deletion of ALL OLD CAMERA CONFIGURATIONS. Work in D:\1\cambida, preserve existing unrelated dirty working tree and ongoing services until safe deployment.
Read project .project files and inspect source/Git first. Check active process/task; do not duplicate. Screenshot user shows localhost:8004/admin, Local camera mode NetSDK 37777 selected yet RTSP preset/advanced visible. User demands audit and correct FULL camera logic: conditional admin fields for local NetSDK vs local RTSP vs NVR, no mixed stale fields in payload/validation, no RTSP fallback when NetSDK explicitly selected, route test/live/snapshot/recording/replay by correct source; regressions for switching modes, adding/removing camera, zero cameras, data reload. RTSP still supported when explicitly selected. NVR isolated.
Delete ALL OLD CAMERA CONFIGURATION ENTRIES in CURRENT ACTIVE settings, NOT just HTML form; identify precise active runtime 8004 process cwd/config locations (workspace and release) before writing. Back up existing configs to restricted local backup with timestamp, do not print or persist passwords in logs/source/.project. Clear camera channel lists and per-camera legacy settings, and any stale cached camera config as appropriate; DO NOT erase global web/Telegram/storage settings, existing recorded videos, or unrelated projects. Do not seed default demo cameras on empty config, ensure zero-channel app/admin renders safely, show add-channel affordance. If service restart necessary, coordinate controlled restart of ONLY verified active Cambida service and verify port 8004 running, new zero-channel state visible; do not kill AGY/Codex or any unrelated processes.
Carefully audit active code 1.py, admin.html, dahua_37777.py, config-loading paths, tests, and runtime generated templates before editing. Real device test only if valid credentials available; do not guess passwords and do not leak secrets. No 3.x version bump without real media snapshot+MP4 proof. Add tests for transport isolation and zero camera support, run py_compile/full tests/rendered JS syntax, smoke admin/API and actual runtime if feasible. Commit only files genuinely in this task without staging unrelated pre-existing edits; if touched file already dirty, carefully isolate staged task hunks or document integration constraint rather than claim completed. Update .project at milestone/final, emit accurate progress via output and final status including changes, deletion scope, backup path without secrets, tests, commit, runtime verification/blockers. Do not exit after a plan: implement, verify, integrate fully. Avoid writing live credentials to prompt, logs, source or tests.
